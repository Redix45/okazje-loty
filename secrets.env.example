# Skopiuj na serwerze do `secrets.env` i wpisz swoje wartosci:
#   cp secrets.env.example secrets.env && nano secrets.env
# Plik secrets.env jest w .gitignore — NIE trafi do repo.

# Webhook Discord (po utworzeniu na kanale: Integracje -> Webhooki):
DISCORD_WEBHOOK=""
